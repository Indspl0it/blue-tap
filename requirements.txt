# Blue-Tap v2.0.1 - Core Dependencies
# Install: pip install -r requirements.txt
#
# System packages required first (Kali/Ubuntu):
#   sudo apt install -y python3-dev python3-pip python3-venv \
#       bluez bluez-tools libbluetooth-dev libdbus-1-dev \
#       libglib2.0-dev libgirepository1.0-dev libcairo2-dev \
#       gir1.2-glib-2.0 pkg-config

click>=8.1
rich>=13.0
bleak>=0.21
dbus-python>=1.3
PyGObject>=3.42

# Optional: fuzzing (protocol-aware packet crafting)
# pip install scapy>=2.5

# Optional: audio capture
# pip install pulsectl>=23.5

# Optional: development
# pip install pytest ruff
