# Blue-Tap v2.1.1
#
# Preferred install: pip install blue-tap
#
# System packages (run first):
#   sudo apt install -y bluez bluez-tools libbluetooth-dev
#
# For source installs: pip install -r requirements.txt

click>=8.1
rich>=13.0
bleak>=0.21
dbus-fast>=2.0
scapy>=2.5
pulsectl>=23.5
